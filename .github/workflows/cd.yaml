name: CD - Terraform Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.8.5

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Terraform Init
        run: cd terraform && terraform init -input=false

      - name: Terraform Plan
        run: cd terraform && terraform plan -input=false -out=tfplan

      - name: Terraform Apply
        run: |
          cd terraform
          terraform import aws_dynamodb_table.resource_tracker resource-tracker-staging
          terraform import aws_iam_role.lambda_execution_role lambda-exec-staging
          terraform import aws_s3_bucket.reports levelup-reports-staging
          terraform import aws_s3_bucket.logs levelup-logs-staging
          terraform import aws_s3_bucket.ai_summaries levelup-ai-summaries-staging
          terraform apply -input=false -auto-approve tfplan
